# Leave unset to use mock/local data everywhere (default — works standalone
# on Netlify with no backend deployed). Once you deploy the backend (e.g. to
# Render), set this to that URL and rebuild:
# VITE_API_URL=https://your-backend.onrender.com/api
